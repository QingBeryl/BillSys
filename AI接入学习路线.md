# BillSys AI 接入学习路线

> 每天一个主题，带着问题来找我，我讲原理 + 你写代码 + 我 review。
> 预计 10 天完成，节奏可以自己调。

---

## Day 1：认识大模型 API

**目标**：理解 LLM API 的请求/响应格式，跑通第一个调用。

**要搞懂的事**：
- 什么是 OpenAI 兼容接口（国内很多模型都走这个格式：通义千问、DeepSeek、Moonshot 等）
- 请求三要素：model、messages（对话历史）、temperature（随机性）
- 响应结构：choices[0].message.content
- 为什么用 Python 的 `openai` 库而不是自己拼 requests（其实也行，但库帮你处理了流式、重试）

**动手任务**：
1. 选一个模型平台注册拿 API Key（推荐 DeepSeek 或通义千问，便宜）
2. 写一个独立的 `test_ai.py`，用 openai 库发一条消息，打印回复
3. 试试把 temperature 从 0 调到 1，观察输出变化

**关键代码骨架**（你自己补全）：
```python
from openai import OpenAI

client = OpenAI(
    api_key="你的key",
    base_url="https://api.deepseek.com/v1"  # 或通义的地址
)

response = client.chat.completions.create(
    model="deepseek-chat",
    messages=[{"role": "user", "content": "你好"}],
    temperature=0.7
)

print(response.choices[0].message.content)
```

---

## Day 2：后端 AI 蓝图搭建

**目标**：在 Flask 后端新建一个 `ai` 蓝图，提供 `/api/ai/parse` 接口。

**要搞懂的事**：
- 为什么 AI 调用放后端而不是前端（Key 安全、prompt 不暴露）
- Blueprint 的注册流程（你已经很熟了）
- 接口设计：前端传什么、后端返什么

**动手任务**：
1. 新建 `backend/routes/ai.py`，创建 `ai_bp`
2. 在 `app.py` 注册蓝图
3. 写一个 POST `/api/ai/parse` 接口，暂时硬编码返回一个假结果
4. 用 Postman 或 curl 测试通了再往下走

**接口契约**（先定好格式）：
```
POST /api/ai/parse
Body: { "text": "昨天中午食堂吃了12块，一卡通刷的" }
Response: {
  "success": true,
  "bill": {
    "type": "支出",
    "money": 12,
    "sub_category": "食品餐饮",
    "account": "校园一卡通",
    "bill_date": "2026-07-30 12:00:00",
    "remark": "食堂"
  }
}
```

---

## Day 3：Prompt 工程——让 AI 输出结构化 JSON

**目标**：设计一个 prompt，让大模型把自然语言解析成账单 JSON。

**要搞懂的事**：
- System prompt vs User prompt 的分工
- 怎么约束输出格式（"只返回 JSON，不要解释"）
- 把可选的分类列表、账户列表塞进 prompt（你的 `/api/meta` 数据）
- 容错：模型返回的不是合法 JSON 怎么办（正则提取 / json.loads try-except）

**动手任务**：
1. 写一个 `build_parse_prompt(user_text, categories, accounts)` 函数
2. 在 `/api/ai/parse` 里真正调用大模型
3. 解析返回的 JSON，校验字段合法性（money > 0、sub_category 在列表里）
4. 测试 5 种不同说法："充了50话费""支付宝转账给小明200块当生日红包""这个月工资到了8000"

**Prompt 参考方向**（你自己组织语言）：
```
你是一个记账助手。用户会用口语描述一笔收支，你需要解析成 JSON。
可选支出分类：[...]
可选收入分类：[...]
可选账户：[...]
规则：
- 只返回 JSON，不要任何解释
- 如果信息不完整，缺失字段填 null
- 日期推断：如果用户说"昨天"，今天是 {today}
```

---

## Day 4：前端对接——自然语言输入框

**目标**：在"记一笔"页面加一个 AI 输入模式，用户打字 → 自动填表。

**要搞懂的事**：
- 前端怎么调后端 AI 接口（axios post，跟调其他接口一样）
- 加载状态处理（AI 响应可能要 2-5 秒，需要 loading 提示）
- 解析结果回填表单后让用户确认/修改，而不是直接提交

**动手任务**：
1. BillForm.vue 顶部加一个输入框 + "AI 识别"按钮（或做成一个可折叠区域）
2. 新建 `frontend/src/api/ai.js`，封装 `parseBill(text)` 方法
3. 点击后调接口，loading 期间按钮转圈
4. 拿到结果后回填 form 的各个字段，弹一个 ElMessage 提示"已识别，请确认"
5. 用户确认后正常走 handleSubmit 提交

**UI 建议**：
- 输入框放在表单最上方，样式做成一个"对话框"的感觉
- placeholder: "试试说：昨天午饭花了15块，微信付的"
- 识别成功后输入框区域可以收起，表单已经填好了

---

## Day 5：错误处理与边界情况

**目标**：让 AI 功能在生产环境下不炸。

**要搞懂的事**：
- 大模型超时怎么办（设 timeout，比如 15 秒）
- API Key 额度用完 / 欠费的错误码处理
- 模型"幻觉"：返回了不存在的分类怎么兜底
- 用户输入完全无关内容（"今天天气不错"）怎么优雅拒绝

**动手任务**：
1. 后端加 try-except，区分网络超时、API 错误、JSON 解析失败
2. 返回统一的错误格式 `{ "success": false, "error": "无法识别，请换个说法" }`
3. 前端根据 success 字段决定是填表还是弹错误提示
4. 加一个简单的输入长度限制（太短没意义，太长浪费 token）

---

## Day 6：AI 月度消费点评

**目标**：Dashboard 上加一个"AI 点评"卡片，总结当月消费情况。

**要搞懂的事**：
- 怎么把结构化数据"喂"给模型（不是让它算数，是让它总结）
- 数据预处理：后端先算好各分类金额、环比变化，拼成文本给模型
- 缓存策略：点评不需要每次刷新都重新生成（可以存数据库，一天生成一次）

**动手任务**：
1. 后端新接口 GET `/api/ai/monthly-review`
2. 后端逻辑：查当月各分类支出 → 拼成一段描述文本 → 发给模型 → 返回点评
3. 前端 Dashboard 加一个卡片，展示点评文字（打字机效果可选）
4. 加一个"重新生成"按钮

**喂给模型的数据格式参考**：
```
本月总支出 3200 元，总收入 5000 元。
支出 Top3：食品餐饮 1200元(37%)、购物消费 800元(25%)、出行交通 500元(16%)。
与上月对比：食品餐饮 +20%，出行交通 -10%。
请用 2-3 句话给出消费建议，语气轻松友好。
```

---

## Day 7：SSE 流式输出（打字机效果）

**目标**：AI 点评不是一次性返回，而是一个字一个字"打"出来。

**要搞懂的事**：
- 什么是 SSE（Server-Sent Events），跟 WebSocket 的区别
- Flask 里怎么做流式响应（`stream=True` + generator + `text/event-stream`）
- 前端用 `fetch` + `ReadableStream` 逐块读取（你的博客项目已经做过了！）
- openai 库的 `stream=True` 参数

**动手任务**：
1. 后端 `/api/ai/monthly-review` 改为流式接口
2. openai 调用加 `stream=True`，逐 chunk yield
3. 前端用 fetch + reader 逐字渲染到卡片里
4. 加一个光标闪烁的 CSS 效果

**提示**：你博客的 AI 聊天已经实现了 SSE，这里逻辑几乎一样，只是数据源从"用户对话"变成了"消费数据总结"。

---

## Day 8：智能分类建议

**目标**：用户手动记账时，根据备注自动推荐子分类。

**要搞懂的事**：
- 这个不一定要用大模型！简单场景用关键词匹配就够了
- 什么时候该用 AI，什么时候规则引擎更合适（成本、速度、确定性）
- 混合方案：先走规则，匹配不上再调 AI

**动手任务**：
1. 后端写一个 `/api/ai/suggest-category` 接口，接收 remark 文本
2. 先做规则层：维护一个关键词映射表（"吃饭/食堂/外卖" → 食品餐饮）
3. 规则没命中时再调大模型
4. 前端：备注输入框 debounce 500ms 后自动请求建议，以下拉提示形式展示

---

## Day 9：预算异常检测

**目标**：当某分类支出接近或超过预算时，AI 生成一条预警文案。

**要搞懂的事**：
- 这个功能的核心其实是后端计算（当前支出 / 预算比例），AI 只负责"说人话"
- 触发时机：不是每次都调 AI，而是比例 > 80% 时才触发
- 可以做成 Dashboard 上的一个警告条

**动手任务**：
1. 后端：查 Budget 表 + 当月实际支出，计算各分类使用率
2. 使用率 > 80% 的分类，拼文本让 AI 生成一句提醒
3. 前端 Dashboard 顶部展示预警条（橙色/红色渐变背景）
4. 示例输出："你的「食品餐饮」预算已用 92%，这个月还剩 9 天，悠着点~"

---

## Day 10：整合、优化与部署考量

**目标**：把前 9 天的功能打磨成一个完整体验。

**要搞懂的事**：
- API Key 管理：环境变量 / .env 文件，不能硬编码
- 成本控制：缓存策略、token 用量估算、设置每日调用上限
- 降级方案：AI 服务挂了，核心记账功能不受影响
- 未来扩展：如果以后用 FastAPI 做 AI 微服务，Flask 主应用怎么调它

**动手任务**：
1. 把 API Key 移到 `.env`，用 `python-dotenv` 加载
2. 给 AI 接口加一个简单的内存缓存（同一个月的点评不重复生成）
3. 前端加一个全局开关：设置页可以关闭 AI 功能
4. 写一段 README 说明 AI 功能的配置方法

---

## 补充：技术选型建议

| 模型平台 | 优点 | 价格 |
|---------|------|------|
| DeepSeek | 便宜、中文好、兼容 OpenAI 格式 | 约 ¥1/百万token |
| 通义千问 | 阿里生态、免费额度多 | 有免费 tier |
| Moonshot (Kimi) | 长上下文、中文理解强 | 约 ¥2/百万token |

对于 BillSys 的场景（解析一句话 + 生成一段点评），每次调用大概 500-1000 token，一天用 50 次也就几分钱。

---

## 学习节奏建议

- 每天花 1-2 小时，先跟我聊原理（15分钟），再自己写代码（45-90分钟）
- 写完发给我 review，我指出问题你改
- 卡住了随时问，但先自己试 10 分钟再问
- Day 1-5 是核心功能（自然语言记账），做完就已经很能打了
- Day 6-10 是锦上添花，可以根据兴趣挑着做
